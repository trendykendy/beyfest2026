#!/usr/bin/env bash
# macOS: double-click this file in Finder to start the tournament app.
# It opens a Terminal window and runs start.sh; close the window to stop.
exec "$(cd "$(dirname "$0")" && pwd)/start.sh"
